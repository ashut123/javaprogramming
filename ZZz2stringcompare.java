public class ZZz2stringcompare {
    public static void main(String[] args) {
        String str1="pryamid";
        String str2=new String("Pyramid");


        System.out.println(str1.compareTo(str2)); // here str1 is less than str2 then it will output as positive value
          // small letter is always greataer than large letter in ASCII code
    
    }
}